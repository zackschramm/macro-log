<svg viewBox="0 0 1024 1024" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1024" height="1024" fill="#0D0D0D"/>

  <!-- Subtle inner frame -->
  <rect x="64" y="64" width="896" height="896" rx="180" fill="#161616"/>

  <!-- Bold geometric F letterform -->
  <!-- Vertical stem -->
  <rect x="300" y="248" width="108" height="528" rx="16" fill="#00C896"/>
  <!-- Top horizontal bar (full width) -->
  <rect x="300" y="248" width="424" height="108" rx="16" fill="#00C896"/>
  <!-- Middle horizontal bar (shorter) -->
  <rect x="300" y="458" width="320" height="96" rx="16" fill="#00C896"/>

  <!-- Accent dot — bottom right -->
  <circle cx="756" cy="756" r="44" fill="#00C896" opacity="0.35"/>
</svg>
